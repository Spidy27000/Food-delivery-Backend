/user/add - POST
args- username, password, email
return
  status,
  userId
  error if status is failed 
note:
  checks for is email in db and gives error if email already in database

/user/:userId - GET
args -user id 
return 
  status,
  data - username, email
  error if status is failed
note:
  error if id not in database

/user/:userId - DELETE
args -user id
return
  status,
  error if status is failed

/user/update - POST
args - userId, username, email, password
returns 
  status,
  error if status is failed
Note: 
  only give the field u want to update as args


/item/add - POST
args - name, descrption, price, image_uri, category, is_veg
returns
  status,
  itemId


/items - GET
returns
  status,
  items - name, descrption, price, image_uri, category, is_veg 
  
/item/:itemId - GET
args- itemId
returns
  status,
  items - name, descrption, price, image_uri, category, is_veg 

/item/search - GET
args - search
returns
  status,
  items - name, descrption, price, image_uri, category, is_veg 
